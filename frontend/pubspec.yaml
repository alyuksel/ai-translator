name: ai_translator
description: "Flutter client for the AI Translator backend (mock integration)."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: "3.9.2"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
